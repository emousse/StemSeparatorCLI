# BeatNet Beat-Service Dependencies
# Environment: Python 3.8 (required for numba 0.54.1 compatibility)
#
# IMPORTANT: Do NOT use pip install -r directly!
# Follow the installation order in README.md:
#   1. conda install -c conda-forge numba=0.54.1
#   2. pip install cython && pip install --no-build-isolation madmom
#   3. pip install BeatNet soundfile torch pyinstaller
#
# Note: This is a SEPARATE environment from the main StemSeparator app

# Install via CONDA (not pip) on Apple Silicon:
# numba==0.54.1  # conda install -c conda-forge numba=0.54.1
# numpy  # installed automatically by conda with numba
# llvmlite  # installed automatically by conda with numba

# Build dependency for madmom
cython>=0.29.0

# madmom - install with: pip install --no-build-isolation madmom
madmom>=0.16.1

# Core beat detection
BeatNet>=1.1.3

# Audio I/O
soundfile>=0.10.0

# ML backend
torch>=1.9.0

# Build tools
pyinstaller>=5.0.0

